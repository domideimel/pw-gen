{
  "private": true,
  "name": "pws-generator",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --preload",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.28.0",
    "eslint-config-preact": "^1.1.4",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.11"
  },
  "dependencies": {
    "preact": "^10.5.13",
    "preact-render-to-string": "^5.1.19"
  },
  "browserslist": [
    "last 1 chrome version",
    "last 1 firefox version",
    "last 1 safari version"
  ]
}
